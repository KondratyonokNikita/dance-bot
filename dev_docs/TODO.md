# TODO

## Автозапуск

- [ ] Настроить `launchd` user-agent для периодического запуска (например, раз в час)
- [ ] Добавить `scripts/com.user.dance-bot.plist`
- [ ] Прописать `PATH` в plist, чтобы сервис находил `claude` CLI и `uv`

## Надёжность данных

- [ ] Таблица `channel_cursors` — явный курсор последнего сообщения по каналу (сейчас вычисляется из `raw_messages`)
- [ ] Обработка `MessageEdited` — обновлять текст в `raw_messages` и перепарсивать при редактировании поста
- [ ] Обновление событий в календаре (`events().update`) при изменении времени, места или цены в посте (сейчас только insert / restore cancelled)
- [ ] Обработка отмены мероприятий — иногда в каналах приходят посты об отмене; научиться распознавать их и снимать/отменять соответствующие события в БД и календаре

## Удобство разработки

- [ ] Отдельные CLI-команды для этапов пайплайна (`fetch`, `parse`, `events`, `sync`) — удобно для дебага без полного прогона

## Расширения (пост-MVP)

- [ ] Telegram-дайджест — форматированные посты со ссылкой на оригинал в личный канал
- [ ] OCR на афишах — если окажется, что много анонсов с датами в картинках
- [ ] Дополнительные источники / города
- [ ] Опция «только будущие события» в календарь (сейчас пишутся все)
